#include <stdio.h>
int main() {
    int array[]={6,3,2,6,4,7};
    int a;
    int duplicate;

    for (a=0;a++;a<6){
    if (array[0]=array[a])
    {
        duplicate=array[a];
        printf("%d", duplicate);
    }
    }
}